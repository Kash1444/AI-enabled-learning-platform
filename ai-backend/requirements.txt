# Full install (RAG + LLM + document processing).
#
# Pinned to versions verified against Python 3.11 - 3.14 on Windows.
# If you are on Python <= 3.12 and want a smaller install, see
# requirements-minimal.txt instead.

# --- Web framework ---------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# --- Settings & validation --------------------------------------------
pydantic==2.13.5
pydantic-settings==2.15.0

# --- Database -----------------------------------------------------------
SQLAlchemy==2.0.52

# --- Document processing ------------------------------------------------
PyMuPDF==1.28.2
python-docx==1.2.0
python-pptx==1.0.2

# --- RAG / vector store ---------------------------------------------------
# chromadb 1.x embeds queries through a separate embed_query() hook; the
# adapter in app/ai/rag.py implements it (and the 0.5.x surface too).
chromadb==1.5.9

# --- Embeddings -----------------------------------------------------------
# sentence-transformers pulls in torch, which has no wheels for Python 3.14
# yet. It is therefore OPTIONAL: leave it out and the platform uses the
# dependency-free hashing embedding provider (EMBEDDING_PROVIDER=hash or
# auto). On Python <= 3.12, uncomment for higher-quality retrieval:
# sentence-transformers==3.3.1

# --- LLM providers (optional; DEMO_MODE works without any of these) ------
anthropic==1.5.0
openai==3.13.0

# --- Testing --------------------------------------------------------------
pytest==9.1.1
httpx==0.28.1

# --- Environment loading (used by pydantic-settings) --------------------
python-dotenv==1.2.3
