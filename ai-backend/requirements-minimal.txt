# Minimal install for a fast, fully offline DEMO_MODE setup.
#
# Skips the LLM SDKs entirely - the LLM abstraction falls back to the
# offline DemoLLMProvider, and RAG uses the dependency-free hash embedding
# provider. Everything the frontend calls still works end to end.
#
# Install with:
#   pip install -r requirements-minimal.txt
#
# Then set in .env:
#   EMBEDDING_PROVIDER=hash
#   DEMO_MODE=true

fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0
SQLAlchemy==2.0.52
PyMuPDF==1.28.2
python-docx==1.2.0
python-pptx==1.0.2
chromadb==1.5.9
pytest==9.1.1
httpx==0.28.1
python-dotenv==1.2.3
